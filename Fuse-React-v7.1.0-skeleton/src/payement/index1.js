import React from 'react';
import ReactDOM from 'react-dom';
import Payment from 'payement/payement'

ReactDOM.render(
	<React.StrictMode>
		<Payment />
	</React.StrictMode>,
	document.getElementById('root')
);